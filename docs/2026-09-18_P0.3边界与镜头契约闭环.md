# 2026-09-18 P0.3：Boundary/Shot Contract Closure

> 第二轮人工审核（p02j）判定：P0.2 修掉的三问题（S2 剪辑理解/humor 落点/
> conflict gate）确认有效，但边界收敛未闭环。四类残留错误 → P0.3 四件套修复，
> commit `e955f57`，717 测全绿，真机结果**待回填**。

## 背景与问题（第二轮人工审核 FAIL 的四类错误）

1. **S1 边界被修坏**：cut_001@5.1 原本合理（Hook 偏见命题 vs 比赛开始是不同
   叙事功能），对账把"同主题"误判成"同事件"移到 6.53，Hook 弱化成"运动员介绍"，
   丢失"失去双手→废人"待反驳命题——改变作品核心逻辑。
2. **S2→S3 边界仍提前**：14.03-16.7 实为击倒后红色头盔近景/反应（比赛
   aftermath），照片蒙太奇约 16.7 才开始；对账只移动一次未复查新边界。
3. **快速蒙太奇后半段未拆完**：detector 已给出 cut_015-cut_022（18.2-21.9s），
   section rewatch 只处理到 cut_015 就把剩余整段吞成一个 shot——"detector 知道、
   Omni 没交作业、系统照单全收"。
4. **转场被吃回内容镜头**：whip_pan T01-T03 已正确识别，却因"有文字"被重分类
   回内容镜头——转场期间挂着字幕不等于内容镜头；且三段 snippet_count_range
   全 [1,1]，与 6 镜头 event_compression 自相矛盾（参考片 6 刀 → 生成端只要
   1 条 Snippet）。

## 候选方案

| 方案 | 思路 | 取舍 |
|---|---|---|
| A | 再赌模型重跑（p02j 同参数） | 非确定性不解决结构性来源 |
| B | P0.3 四件套：迭代式帧级边界对账 + 候选切点全量交作业 + 转场身份保持 + snippet 结构对齐（用户拍板） | 改动集中、每条都有确定性锚 |

## 选择与理由（用户拍板 B）

1. **迭代式边界对账（reconcile-until-stable）**：每个内部边界先过确定性规则
   （下一 Section 首 shot 报 same_event 即事件内部→直移），否则抽边界前后各
   三帧（±2/±1/±0.4s）做画面级**三维度**判定：event 连续性 /
   rhetorical_function 连续性（提出命题 vs 提供反证 vs 扩展证据 vs 收尾打趣）/
   audience_cognition 连续性，prompt 明示"同主题≠同事件"。不成立→移到下一
   候选边界→**复查新边界**→直到真实语义变化或候选耗尽（BLOCKED），最多
   RECONCILE_MAX_ITERATIONS=4 轮。移动后受影响 Section 重看。
2. **候选切点全量交作业**：`_observe_section` 新增硬校验——输入
   cut_candidates_to_assess 的每个 boundary 必须出现在 cut_assessments
   （real_cut/not_cut/uncertain 三选一），缺失即 V9Blocked
   `cut_assessment_missing`。"18-22 秒被吞成一个 shot"从此不可能静默发生。
3. **转场身份保持**：`apply_montage_reclassification` 重写——转场永远是
   transition，逐镜头观察只补 transition_type 与 overlay_text 属性
   （转场期间挂字幕=叠加层，不是内容）。删除升回内容镜头的逻辑与
   `_aligned_montage_for_payload`（id 天然稳定）。
4. **snippet 结构对齐**：蒙太奇类模式（micro/event_compression/evidence/
   multi_angle/contrast/text_led_montage）的 snippet_count_range 程序侧强制
   `[max(2, ceil(0.6n)), n]`（n=观察到的内容镜头数，记录 basis）；requirements
   新增 reference_content_shot_count / reference_transition_count；validator
   加"蒙太奇 snippet 下限≥2"硬规则。
5. **Hook 命题保全**（用户第五条）：CONTENT_PROGRAM_PROMPT 新增
   hook_text_quote——第一个含屏幕文字的 Section 必须逐字引用断言文字并在
   takeaway 中呈现为全片待检验命题，不得降级为人物介绍；validator 逐字校验。

## 实测/对比

- 版本升 `_p03`（V9/Content/Edit/各 stage schema）。
- 测试 713→**717**：重写迭代对账三态测试（帧判定保留/两次判定移动到稳定/
  候选耗尽 BLOCKED）、same_event 直移+run 末端帧复查、转场 overlay 保留
  （不再升级）、蒙太奇 snippet 下限与 0.6n 对齐、hook_text_quote 三态。
- 途中修掉设计瑕疵：迭代中 next_row 观察是陈旧数据（重看只发生在接受后），
  因此 run 末端边界必须靠帧判定收口——按设计接受并用测试锚定。
- 真机 P0.3 轮（p6_v9_p03_20260918）：**待回填**。
- p03 首跑：`cut_assessment_missing` 硬门**首次上岗即拦下** section_03 九个未交作业切点
  （cut_014-022）——证明"detector 知道、Omni 没交作业"从此不可能静默；随后实现
  **密集切点逐对帧补判**（每切点 ±0.25s 帧对、3 切点一批 inspect，
  `7c4b06d`）。
- p03b：密集蒙太奇完整展开（S3=8 内容镜头+7 whip_pan 转场、snippet [5,8]、
  hook 逐字引用）；但暴露两 bug：①snippet 对齐顺序（模式矫正后 snippet 检查仍用
  旧模式，S1/S2 漏网 [1,1]）；②same_event 直移在 S1 又误移 5.1→6.53。同时
  **用户揪出提示词泄漏**：边界判定模板示例含参考特定内容"跆拳道"（禁词表成员），
  等于把本片修辞结构预喂给判定器（`0bbd3f8` 泛化 + 12 个 prompt 模板全量
  spoiler 测试）。
- p03c（中性 prompt）：5.1 正确保住，但 13.0 被帧判定以"修辞变化（决胜时刻）"
  放行——暴露根本设计问题：**修辞变化≠事件边界**。事件的 setup/attack/result/
  reaction 是 phases，不是 Section。杀跑改闸（`ef91ad7`）：
  **边界成立 ⟺ 帧判定 event_continuity=False（新事件开始）**，修辞/认知维度
  只记录不放行。
- p03d（`ef91ad7`，事件硬闸）：模型四连答照抄示例值（reason="画面依据"）→
  echo 硬闸 + 占位文案反制（`a798a15`）。
- p03e/f（`8ed102e`）：加入操作性事件定义后模型把整片看成**一个事件**
  （event_continuity=true 连 5.1 都不认）——欠分割。至此过分割（p03c@13.0）
  与欠分割（p03e@5.1）在同一把"事件"尺子下轮流翻车。

## P0.3F：层级叙事边界分类器（用户研究诊断拍板）

用户引用 GEBD / Event Segmentation Theory / Event Indexing Model /
MovieScenes / NarraScene / Video Plot Segmentation / DiffGEBD 定性根因：
**目标粒度错配**——flat `event_continuity` 布尔没有指明"哪一层的事件"。
Section 应是**叙事/修辞单元**（内部可含多镜头、多 phase、甚至多个局部事件，
只要服务同一表达目的），不是物理事件单元。

文献代码现状（联网核实）：GEBD（ramyamounir/GEBD-1）与 SceneSeg/LGSS
（anyirao/SceneSeg）是检测器训练管线，无 LLM 判定协议可搬；NarraScene
（arXiv 2608.28699，三级认知分类 physical/character/narrative，
**"every valid boundary requires a narrative-level shift"**）未放码。
可复用的是它们的**分类协议与决策规则**：NarraScene 三级分类 + 叙事层硬规则、
GEBD one-level-deeper 目标层级、SceneSeg local-to-global 上下文。据此落地：

1. **LLM 只描述，代码终判**：`HIERARCHICAL_BOUNDARY_PROMPT` 让模型输出
   `before/after {episode, phase, rhetorical_role}` + `relations {same_session,
   same_episode, phase_progression, rhetorical_shift}`；
   `_hierarchical_boundary_decision` 按用户伪代码原样拍板：
   rhetorical_shift→keep；new_episode+role 变化→keep；phase_progression→
   reject；same_episode→reject；否则 unresolved→移到下一候选继续。
   S3 证据蒙太奇内部（新事件但叙事功能相同）天然落入 unresolved，不再拆段。
2. **local-to-global**：粗读草稿的 core_expression + unit purposes 作为全局
   叙事纲要随帧下发（不含任何边界信息，spoiler 测试锚定）。
3. **echo 硬闸扩展**：before/after 描述字段照抄模板占位文案同样 BLOCKED。
4. 预期锚定（用户给的标尺）：5.1=keep（前提→反证）、13.0=reject（action→
   result phase）、16.x=keep（反证→证据扩展）；决策树三案例 + 并列证据
   unresolved 全部进了单测。

测试 720→**724**。真机 P0.3F 轮（p6_v9_p03f2_20260918）：**待回填**。

## 验收判据（用户五条硬线）

1. Hook 仍是"失去双手→废人"偏见命题（hook_text_quote 逐字）；
2. 完整跆拳道事件不截断（setup/attack→result→reaction 同一 Section，
   边界应收敛到 ~16.7 照片蒙太奇开始处）；
3. 后半快速蒙太奇每个候选 cut 都有归属（cut_assessment_missing 硬门）；
4. transition 真正进入 Edit Program，不再被当内容镜头；
5. 蒙太奇不再出现 snippet_count=[1,1]。

## 从结果学到什么

1. **确定性信号必须有权威**：same_event 是复看层亲眼所见的事实，模型的对账
   判断只能被它否决，不能反过来（P0.2 教训的延续）。
2. **移动一次不叫收敛**：任何边界修改都必须复查新边界，直到稳定——
   单步修复在结构问题上只是把错误挪了个位置。
3. **双重属性用字段表达，不用类型翻转**：转场+叠加文字是
   `segment_kind=transition, overlay_text=...`，不是"变成内容镜头"。
   类型翻转会摧毁下游对结构的全部计数（snippet、镜头数、节奏）。
4. **数量契约要双向锚定**：参考侧记 reference_content_shot_count，生成侧记
   target_snippet_count_range，两者失配在 validator 一眼可见。
