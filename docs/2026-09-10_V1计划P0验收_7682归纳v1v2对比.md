# 2026-09-10 V1 计划 P0 验收：7682 叙事归纳 v1 vs v2（prompt v2 + 依赖缓存）

## 验收方法

参考身份核对先行：reference_identity.json 确认 reference_id ↔ 文件 ↔ sha256
（2f95e24e…）↔ 标题（良田·父亲打磨奇迹）一致——不拿计划描述当核对标准。
随后逐字段对比新旧归纳（v1=旧缓存，v2=SYNTHESIS_PROMPT v2 重算，窗口观察复用）。

## 结果（五项设计目标四项直接命中，一项按设计转为待复核）

| 设计目标 | v1 | v2 |
| --- | --- | --- |
| 主角对账（不预设答案） | 断言「女性运动员展示自信」 | 「女性，**未见明显肢体缺失**」+ uncertainties：「是否实际失去双手尚无画面或 OCR 支持，仅通过旁白提及，**需复核**」——模型间分歧（帧分析 vs 看片）被如实上报而非硬猜 |
| 六问诚实（unknown/not_applicable） | topic/message 泛化断言 | motivation=unknown、goal=not_applicable、outcome=unknown，无编造动机 |
| 事件多角色（禁复制凑弧） | hook 与 conflict 同引 event_002（撞车） | 每段独立事件 + function（「提出社会偏见，建立观众认知冲突」） |
| 证据分级 | — | evidence source 只有 video/ocr/audio；标题未被当作事实（评论区为空） |
| importance | 无 | high/medium 落位，program status=uncertain（诚实） |

身份核对的附带发现：用户亲自看帧描述与 Omni 看片结论在主角性别/上肢上存在分歧
（道服对练画面 vs 握手画面），v2 的处理正是设计要求——可见特征写 protagonist、
分歧进 uncertainties 请求复核。**分歧本身被保留为证据，不被任何一方覆盖。**

## 机制验证

- 依赖缓存：v2 重算只重放归纳（窗口观察 v1 缓存命中，observation 键含源哈希+
  区间+提示词哈希）；信封带 prompt_versions {window: v1, synthesis: v2} 与
  synthesis_key；旧无 cache 信封一律判陈旧（7668 同批重算中）
- 参考身份文件随 run 落盘（decompose_v2/reference_identity.json）

## 遗留

- 21.9s 收尾梗「但不会剪脚指甲」仍未进弧（OCR 尾帧事件，属"创作者陈述"层）
  ——P4 的 Presentation/文案轨承担该层，不硬塞进事件弧
- 评论区为空（comments=[]）：external_comment 通道已就绪待 DOUYIN_COOKIE 补抓
- v2 弧仅 2 段（hook/context）→ 下游 _expand_thin_arc 补位，符合预期

## 结论

P0 通过。归纳层从"编故事"转为"报证据"，主角对账/证据分级/多角色规则全部生效。
