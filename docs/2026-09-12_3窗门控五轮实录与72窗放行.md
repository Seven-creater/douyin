# 2026-09-12 3 窗 pack-on/off 门控五轮实录（72 窗启动门）

## 结论：第五轮五条判据全过，72 窗全量放行（20:16 起跑）

| # | 判据 | r5 结果 |
|---|---|---|
| 1 | 同一人物跨窗 canonical 一致 | 无限三窗全 char:wuxian[supported@0.9]（持刀青年/倒提他人/制服青年，证据各窗独立）；小黑三窗全 char:xiaohei |
| 2 | hard negative 不误绑 | w19 小黑绑 xiaohei 未碰 wuxian |
| 3 | 事件不被知识改写 | 两模式事件几乎逐字一致，零剧情泄漏 |
| 4 | supported 真实产出 | 6 条 supported 全真值 canonical 全带证据 |
| 5 | 无内容漂移 | names 一致（w24 pack_on 按纪律多枚举一实体） |

## 五轮抓出的 9 个 bug（全部会乘以 24 倍污染全量）

| 轮 | 发现 | 修复 commit |
|---|---|---|
| r1 | franchise 断链（视觉拒认→char:unknown:*）；幻觉 slug（无限→zhongli）；parse 失败仍记 completed（断点污染） | 27b9e9b |
| r2 | shot_idx 重编号整窗静默丢；metadata_prior 幻觉 roster（12 实体 9 个编造）兜底乱绑 supported@0.9 | ea99d94 |
| r3 | pack-off 无白名单时占位符原文当 canonical；成人写成少女；单主体漏标 | 5d2c682（prompt v3） |
| r4 | pack appearance 模型记忆不可靠（无限白发长袍实为黑发青年）→ 标注器守纪律拒绝绑定 | 25b71e6（grounded 用手写 registry 的 role/appearance 提示） |
| r5 | （全过） | — |

## 关键设计决定（沿路拍板）

- 门控窗口专门设计成身份小测试：A=w24(无限) B=w14(无限+小黑) C=w19(小黑 hard negative)——GT 靠人工帧审（w12 帧审纠错：持刀对峙少年是小黑非无限）
- metadata_prior 只注入 grounded（命中手写 registry）实体——幻觉条目留档不进候选表
- 外观别名的新正当用途：绑定引导（prompt 参考），与身份判等（build_alias_maps 只吃 aliases）分离
- pack-off 模式 bindings 整层丢弃（没有候选表就没有 canonical 概念）
- 单 runner 复用、setsid 脱离、ssh 长连接会断（55min 实锤）——链路必须服务端自驱

## 72 窗链（night_72.py，20:16 起）

两片 force 全量重标(prompt v3+新 pack) → update_pack_from_annotations 回填
(supported 上限) → auto registry → index 重建 → V4 成片(lxh_p4_V4_C2)。
旧标注备份 narrative_annotations.pre_v3.json。日志 data/agentic_runs/night_72.log。
